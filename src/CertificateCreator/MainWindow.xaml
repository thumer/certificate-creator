<Window x:Class="CertificateCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CertificateCreator"
        mc:Ignorable="d"
        Title="Certificate Creator" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Country:" Grid.Row="0" Margin="0,0,5,5"/>
        <TextBox Text="{Binding Country}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

        <TextBlock Text="State:" Grid.Row="1" Margin="0,0,5,5"/>
        <TextBox Text="{Binding State}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

        <TextBlock Text="Locality:" Grid.Row="2" Margin="0,0,5,5"/>
        <TextBox Text="{Binding Locality}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>

        <TextBlock Text="Organization:" Grid.Row="3" Margin="0,0,5,5"/>
        <TextBox Text="{Binding Organization}" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"/>

        <TextBlock Text="Common Name:" Grid.Row="4" Margin="0,0,5,5"/>
        <TextBox Text="{Binding CommonName}" Grid.Row="4" Grid.Column="1" Margin="0,0,0,5"/>

        <TextBlock Text="Expiration:" Grid.Row="5" Margin="0,0,5,5"/>
        <DatePicker SelectedDate="{Binding Expiration}" Grid.Row="5" Grid.Column="1" Margin="0,0,0,5"/>

        <CheckBox Content="Root Zertifikat erstellen" IsChecked="{Binding CreateRoot}" Grid.Row="6" Grid.ColumnSpan="2" Margin="0,0,0,5"/>

        <Button Content="Erstellen" Command="{Binding CreateCommand}" Grid.Row="7" Grid.ColumnSpan="2" Height="30" Width="100" HorizontalAlignment="Center"/>
    </Grid>
</Window>
